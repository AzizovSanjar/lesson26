
const BASE_URL = `http://localhost:3030`;

export const endpoints = {
    users: BASE_URL + "/users",
  };

const URL = `http://localhost:3030`

export const endpointss = {
posts: URL + '/posts'
}


